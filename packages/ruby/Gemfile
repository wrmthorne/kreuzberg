# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

gem 'pry', '~> 0.14', group: :development

platforms :x86_64_linux, :arm64_darwin do
  gem 'pry-byebug', '~> 3.10', group: :development
end
